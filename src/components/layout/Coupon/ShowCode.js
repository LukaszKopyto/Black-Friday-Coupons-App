import styled from 'styled-components'

export const ShowCode = styled.button`
  font-size: 16px;
  font-weight: 300;
  color: ${({ theme }) => theme.colors.textColor};
  background: #fff;
  border-radius: 1.25rem;
  border: 0;
  width: 90%;
  padding: 9px 17px;
  box-shadow: 0 11px 22px 0 rgba(0, 0, 0, 0.1);
`
